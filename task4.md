All the tooltips - When moused over and they are at the bottom of the page, the hover text is not fully visible. So center the hover text on the visible screen by the side of the mouse.

Some corrections:
In User Authentication Physical Architecture
- The cosmos db should be inside the vnet.

Hover text for cosmos db, correct it to say the following-
It is used for storing additional user data along with data regarding the organizations they belong to. It also stores meta data about organizations, applications and federation provider.

cosmos db is backed up to east us. But at runtime is served only from west us.

Make this change in both administrative and authentication physical architecture tabs.

